# SpamAssassin Service Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=production

# Security - Generate a strong random string for API_SECRET_KEY
# Example: openssl rand -hex 32
API_SECRET_KEY=your-secret-api-key-here-change-this

# CORS - Comma-separated list of allowed origins
# Use * for wildcard subdomains: https://*.vercel.app
ALLOWED_ORIGINS=http://localhost:3001,https://*.vercel.app

# Rate Limiting
RATE_LIMIT_MAX=100
